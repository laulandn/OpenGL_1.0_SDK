
CC=m68k-apple-macos-gcc
CXX=m68k-apple-macos-g++
#CC=powerpc-apple-macos-gcc
#CXX=powerpc-apple-macos-g++

CFLAGS= -I../../../../Headers
LDFLAGS= -lglut -lOpenGLUtility -lOpenGLLibrary -lDrawSprocketLib -lm

OBJS=$(subst .c,.o,$(wildcard *.c))

.c.o:
	$(CC) $(CFLAGS) -c -o $@ $<

all: $(OBJS)
	$(CXX) -o raceGL.code.bin $(OBJS) $(LDFLAGS)
	../bin2app.sh raceGL
#	$(CC) -o raceGL $(OBJS) $(LDFLAGS)
#	$(HOME)/sdl2macos9/xcoff2app.sh raceGL

clean:
	rm -f *.o *.bin *.pef *.APPL *.dsk *.gdb raceGL
